# Metalografías fuera del entrenamiento del modelo

A continuación, se presentan metalografías que no vio el algoritmo de clustering en su entrenamiento, se recomienda probarlas en la interfaz gráfica a nivel de frontend.